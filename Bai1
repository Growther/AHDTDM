#include <stdio.h>

int main() {
    // Lặp qua tất cả các số nguyên có hai chữ số
    for (int i = 10; i < 100; i++) {
        // Kiểm tra xem số đó có phải là bội của 7 không
        if (i % 7 == 0) {
            // In số đó ra màn hình
            printf("%d\n", i);
        }
    }
    return 0;
}
